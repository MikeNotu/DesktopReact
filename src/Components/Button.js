import React from "react";

const styles = {};

const Button = () => {
    return (
        <button style={{ backgroundColor: "royalblue", color: "#fff" }}>
            Test
        </button>
    );
};

export default Button;
